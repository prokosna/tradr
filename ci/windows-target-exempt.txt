# ci/windows-target-exempt.txt
#
# Crates holding Windows cfg gates exempt from host MSVC compilation.
#
# One entry per line, two fields separated by "|":
#   crate | reason
#
# Blank lines and lines starting with "#" are ignored.
# An entry whose reason is empty or whitespace-only makes the check fail:
# the escape hatch cannot be taken silently.
#
tradr-app | MSVC build of ring requires lib.exe not supplied on Linux host; compiled by windows CI job
tauri-plugin-tradr | MSVC build of blake3 requires ml64.exe and ring requires lib.exe; compiled by windows CI job
tradr-discovery | MSVC build of blake3 requires ml64.exe not supplied on Linux host; compiled by windows CI job
