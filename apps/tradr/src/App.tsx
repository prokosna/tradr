// A single component confirming the Tauri shell has started. Design and
// the rest of the UI belong to a later Work Item; this one proves the window opens.
export function App() {
	return (
		<main>
			<h1>Tradr</h1>
		</main>
	);
}
