## WI-M1-026: The UI surface and M1 completion

### Target
`apps/desktop/src/`
`crates/tauri-plugin-tradr/src/lib.rs` (if events/setup needs slight tweaks)
`crates/tauri-plugin-tradr/src/commands.rs` (if progress event generation is missing)

### Design to consult
- docs/01-overview.md, "The Drag-and-drop target"

### Definition of done
- [ ] Connect the React frontend to `get_peers`. It should display a list of discovered peers (Device ID or Display Name).
- [ ] Implement a full-window drag-and-drop target in React (`apps/desktop/src/`).
- [ ] When files are dropped, allow selecting a peer from the list and invoke the `send_files` Tauri command with the selected `peer_id` and the dropped file paths.
- [ ] Implement progress events (`transfer-progress`) in the backend (if not fully implemented in M1-025) and listen for them in the frontend to display a basic progress bar or status text.
- [ ] The app runs cleanly via `cargo tauri dev` or `cargo tauri build`, passing all frontend linters (`pnpm lint`, `pnpm typecheck`).

### Constraints
- Do not modify `tradr-core`, `tradr-transport`, or `tradr-discovery`.
- Keep the UI minimal and functional (a peer list, a drop zone, a progress indicator) to satisfy M1 without over-engineering the styling yet.
- Use Tauri v2 IPC commands `invoke` and `listen`.

### Prohibitions
- Editing `docs/`
- Editing `STATE.md`, `AGENTS.md`, or `CLAUDE.md`
- Committing
- Anything absent from the definition of done

## Verdict: PASS
The implementer did an excellent job wiring the UI, commands, and even writing the backend progress mechanism `send_file_with_progress`.
The implementer timed out at the end, and there were two Biome lint errors relating to React hooks exhaustive dependencies, which the Supervisor fixed by separating the interval logic into a distinct useEffect block and applying `useCallback`.
Passed all frontend and backend checks locally. Ready for merge.
