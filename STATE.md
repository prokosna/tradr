# STATE

> **Only the Supervisor edits this file.** Update it after each review, before anything else.
> **Everything a session needs after it has arrived is in [RECORD.md](RECORD.md)**, its peer at the repository root: the Review record, the Design Changes register, closed milestones, and findings whose repairs have landed. `ci/state-sync.sh` reads both files, so moving a section there does not take it out of any check. **When this file grows past its ceiling, the answer is to move a closed section across, never to shorten one.**
> An arriving Supervisor reads this first, then runs `git log --oneline -20` to see what happened after `last_updated`.
> **Commits newer than `last_updated` mean the first job is reconciling this file.** `branch`, `work_items_landed`, and `last_commit` are no longer declared here — see [docs/10](docs/10-implementation-process.md#the-yaml-header--what-is-in-it-and-what-was-removed-dcr-060) for the one-command equivalents.

```yaml
last_updated: 2026-09-21
phase: implementing
current_milestone: M8
implementation_started: true
repo_initialized: true (pushed to git@github.com:prokosna/tradr)
```

---

## Where we are

> **Everything below this line is derivable from somewhere else, so it is not written here.** What exists is the Work Item table. How many crates there are is `ls crates/`. Which rules hold is `ci/run-all.sh`. **A prose inventory in this spot has now gone stale four times, the last time while carrying a warning that said it goes stale** -- so the inventory is gone rather than corrected a fifth time, and what remains is the two things nothing else records.













**The single most useful thing to read next is the Review record.** It carries why each Work Item went the way it did, and most of its `REVISE` entries were caused by an error in the Supervisor's own Work Order rather than by the Implementer. That ratio is the main finding of M0 so far, and the `DISCARD` entry is the sharpest instance of it.

## Next three actions

> **All three are device runs, and the exact procedure for each is in [RECORD.md](RECORD.md), under "Device runs: the exact procedure".** What is written below is why each one matters; what is written there is how to perform it and what to write down. **The two were separated on 2026-09-19 because this file is under its ceiling and that one is live reference**, sitting beside the Build environment section every step of them depends on.

1. **Run A is unblocked and performing it is the next action.** It could not be attempted on 2026-09-20: `tradr-cli device` answered `SS Error: object locked` before any network existed to test. DCR-143 ruled both defects that day and `WI-M8-031` and `WI-M8-032` landed them; **both are verified on this machine on 2026-09-21 rather than assumed**. `tradr-cli device` answers `device id 182b685b86a9d851c9f47aecc2f4dc93`, `backing software (no Secret Service session available)`, `storage file` -- **and that Device ID is the one the 2026-09-19 run reported the sender seeing**, so the ladder adopted the existing identity rather than minting a second, which is the property the whole ruling turns on. `tradr-cli receive` now prints the authorization url and `ssh -L 21821:localhost:21821 USER@HOST` and goes on waiting instead of aborting. **What the run needs from a person**: this machine as receiver with that forward in place and the url opened on the MacBook, the MacBook as sender with the same Google account, **no Static Peer added by hand**, and the sending side can be the GUI or `tradr-cli send`. The procedure is in [RECORD.md](RECORD.md) under "Device runs: the exact procedure"; what it measures is whether the hand-added Static Peer is still needed, which is the one question M8's completion criterion is waiting on.

2. **`WI-M6-009` is the last planned row that the run above is a precondition for, and it is not what the milestone is waiting for.** (`WI-M8-026` is also planned and is the deferred-findings backlog rather than the milestone: DF-86 ruled by DCR-137.) A QR read by any camera application already links (docs/11: "one payload and one parser"), so a scanner inside Tradr is a platform integration rather than an interface change -- Android has `@tauri-apps/plugin-barcode-scanner` and a camera permission to justify, and the desktop has no camera API at all. **Cut it after the run above, not before**: what the run turns up is what M8's completion criterion is actually short of, and a row cut ahead of that measurement is a guess about where the remaining work is. **`WI-M8-018` landed 2026-09-18 and was the last row a machine could finish without a radio in the room**, which is what makes action 1 the only thing between here and the milestone's question.
3. **Run B was performed on 2026-09-19 and M7's own criterion is now blocked on hardware rather than on effort.** One press of Sign In on an `SM-S928Q` returned `sign_in: option_class=googleId`, and the sign-in completed -- which is the nonce measurement, since the path refuses anything but `SameAccount`. **The renewal measurement was taken and it is negative**: `SignInState` is memory-only, so `am force-stop` brings the button back, and the press after a restart showed an account chooser before answering `googleId`. So a restart costs an interaction; nothing silent was observed. The Device ID was unchanged across the restart, `5ef366a5...2920`, which is the control that says `force-stop` left the key alone. **Two things fell out of it, DF-90 and DF-91.** **Run C cannot be performed with the hardware here.** The receiver must advertise and only Android can (R1: this machine's `hci0` refuses an empty advertisement at the kernel's MGMT layer; macOS and Windows have no BLE implementation in the tree at all, `bluer` being Linux-gated and `ble_linux` the only module). The second Android is a Kindle Fire, which DF-91 rules out. **Parked 2026-09-19 at the user's direction: the devices here do not use BLE, so the run is not worth the hardware it needs.** That changes no code -- M7 closed on its code by DCR-117 and every row is landed -- and it is carried rather than dropped because decision 14 is what settles whether it is ever run. **DF-91 was named as the way back to it without new hardware** -- a Kindle that can sign in is a second Android -- and **that route closed on 2026-09-20 by DCR-140**: the browser flow that would have let it sign in was already refused by DCR-112, so the Kindle cannot be the second Android and its BLE peripheral support never needed measuring. The other shape, measured before it was parked, is the desktop PC booted to Linux as the sender -- scanning is what R1 measured working, 139 reports in twelve seconds -- with the Galaxy advertising, and it needs a Tradr build on that machine first.

## In flight

**`WI-M8-033`, ruled by DCR-144 on 2026-09-21.** The docs commit is ahead of it per [CLAUDE.md](CLAUDE.md#5-git--only-the-supervisor-commits). It is DF-99's repair, and DF-99 is what cost Run A its first attempt.

## Decisions

**Settled decisions are in [RECORD.md](RECORD.md).** What is here is what is still open.

### Open

| # | Decision | Needed by | Who decides |
|---|---|---|---|
| 15 | **Whether the storage ladder keeps a plaintext rung.** Raised by the user on 2026-09-20: if a `0600` file rung exists at all, the at-rest protection the Secret Service rung is there for has already been conceded for any device that lands below. **The measurements are in DF-96 and they favour the argument** -- the top rung's protection is conditional on a non-empty keyring password, holds only against offline attackers, and which rung a device lands on is decided by circumstance and then frozen, since promotion is forbidden. **The exit nobody has costed is a passphrase-encrypted file rung**, the shape an ssh private key has, which protects both rungs at rest and works headless. **Nothing is being proposed yet**: it reaches Layer 0 and `SecretStore`, and DCR-143 deliberately fixed the availability defect without touching it | Before M9 | User |
| 14 | **Whether BLE ships at the priority M7 gave it.** The user said on 2026-09-19 that the devices here do not use it, and M7's completion criterion has never been run -- it cost the roadmap's largest estimation risk, four to five weeks, and every row of it is landed and green. **Nothing is being proposed here**: the code exists, `no-brokr` and the Tier 0/1 inventory cover it, and removing it would be a larger change than leaving it. **What is undecided is whether it is ever exercised**, and the honest answers differ -- a capability that is shipped and never run is one whose first real user finds its defects, which is what Run A just demonstrated for QUIC. **It is carried rather than forced**, because the cost of deciding it now is higher than the cost of leaving it. **Its cheapest resolution closed on 2026-09-20**: DF-91 was the route back to Run C needing no new hardware, on the reading that a Kindle which can sign in is a second Android, and DCR-140 rules that it cannot and will not. What is left is new hardware or the desktop PC booted to Linux as the sender | Before M10, or the first time BLE costs a Work Item | User |
| 13 | ~~Whether Android gets the hardware-backed Device Key docs/05 promised~~ **Decided 2026-09-19: the table is corrected, not implemented.** And the measurement that settled it found the gap was three times what DF-90 named -- Android, macOS and Windows all fall to a `0600` file, and only the Linux row was ever true. **A false document is today's defect; a missing capability is a roadmap item**, and conflating them would have put a Layer 0 change ahead of DF-24. The intended design stays in docs/05, unimplemented and unscheduled, with the obstacle named | -- | Decided |
| 10 | Whether one device may hold several Google accounts | M6 | User |
| 11 | Transfer history retention, and the default write limit for a writable Share | M3 | Open |

### Build environment, Ubuntu 24.04.4 LTS

**In [RECORD.md](RECORD.md), and read it before dispatching anything** -- it is what says which tools exist and what they can do, and a stale sentence in it has already cost two self-inflicted `REVISE` rounds. The paragraph explaining why the move was not a demotion went across to sit beside the section it describes on 2026-09-13, at the eighth ceiling.

---

## Milestones

From [docs/09-roadmap-and-risks.md](docs/09-roadmap-and-risks.md).

| # | Content | Estimate | Status |
|---|---|---|---|
| **M0** | **Skeleton** — monorepo, Tauri launching on Linux and Android, Google sign-in, key generation, Attestation issue and verify | 2 weeks | **done 2026-08-25** |
| M1 | **LAN transfer**, the most important — mDNS, QUIC, transfer, resumption, drag-and-drop send | 4 weeks | **done 2026-08-29** |
| M2 | Android integration — share sheet, Sharing Shortcuts, SAF, permissions | 3 weeks | **done 2026-08-30** |
| M3 | Share browsing — VFS, boundary enforcement, the Browse plane | 3 weeks | **done 2026-08-30** |
| M4 | Windows and macOS — builds, signing, auto-update, tray | 3 weeks | **done 2026-08-30** |
| M5 | Static Peers and overlay networks — direct over Tailscale | 1 week | **done 2026-08-31** |
| M6 | Account linking — QR, Link Secret, Fingerprint | 2 weeks | **done 2026-09-05** |
| M7 | BLE, the largest estimation risk — advertising and scanning on four platforms, EIDs, Noise over GATT | 4-5 weeks | **done on its code 2026-09-14, DCR-117.** Its completion criterion is a run on two radios and that run is carried into M8 |
| **M8** | **Usable interface, and a CLI** — the two open interface defects, whatever running it on all four devices turns up, and a command-line front end. **The CLI is Change Drill D9 performed for real**, and it was measured rather than estimated: every function in `commands.rs` above the first `#[tauri::command]` is already Tauri-free, so it is a move and not a rewrite. **Receiving is the undecided part** — a foreground command and a daemon are different products. **Added 2026-09-13 by DCR-113: the roadmap scheduled capabilities and never scheduled this, and it is the condition on starting to use the software** | unestimated | **current** |
| M9 | **Brokr** — presence, rendezvous, relay, FCM, revocation list, **and Deferred Delivery**, which DCR-113 records as wanted and undesigned | 3 weeks | todo |
| M10 | Finishing — internal security review, one Linux package, i18n, resumption tests. **Store submission, four package formats and purchased certificates are dropped**, closing decisions 7 and 8 and risks R5, R9 and R11 | ongoing | todo |

### Current milestone: M8, a usable interface and a CLI
**Design**: [docs/09-roadmap-and-risks.md](docs/09-roadmap-and-risks.md), [docs/02-architecture.md](docs/02-architecture.md)
**Done when**: the person running it sends and receives on all four devices without needing to know what a Static Peer or a Trust Tier is.

**M7 closed on its code and not on its run, decided 2026-09-14 by DCR-117.** Its completion criterion asks for two devices exchanging a file with every Wi-Fi off, and that run has never been performed; every row that a machine can implement is landed, every gate is green, and the two things left are an APK built on the MacBook and two radios in one room. **Closing on the code is the honest reading of what happened rather than a concession**: M7 was the roadmap's largest estimation risk because nobody knew what BLE would cost to write, and that question is answered -- what is unanswered is whether the thing works, which is a different question and the one M8 exists to ask. The two runs are carried into this milestone as actions rather than as Work Items, because neither of them produces a diff. **What closing it also settles is decision 6**, which was written to settle at M7's end and does: `agy` for determinate and Critical-Module Work Items, with the six-part caveat the settled row carries in [RECORD.md](RECORD.md).

**The CLI is what this machine can build, and DCR-118 gives it a shape.** [docs/09](docs/09-roadmap-and-risks.md) measured on 2026-09-13 that the CLI is a move rather than a rewrite -- every function in `commands.rs` above the first `#[tauri::command]` names no Tauri type, and ten of the plugin crate's modules mention Tauri not once. **What the measurement did not check is whether a crate could hold them, and it cannot as the rules stand**: `ci/layer-deps.sh` check 4 permits an implementation crate only `tradr-core` and `tradr-proto`, and the Tauri-free half reaches six internal crates, so a crate holding it fails the gate on its first commit. **That is the design change and it is a one-word one**: the composition tier has two crates rather than one, `tradr-app` and `tauri-plugin-tradr`, and check 4 exempts both. **Check 3 exempts only the second**, which is what turns the D9 reading into a gate -- the shell-free half stops being Tauri-free because a grep said so and starts being Tauri-free because a manifest check refuses the dependency.

**The extraction goes in four phases, cut so that each one leaves a compiling tree and a green gate.** Phase 1 is `WI-M8-001`: the five modules that name neither Tauri nor a sibling module -- `capabilities`, `handshake`, `link_exchange`, `share`, `transfer` -- plus the rule change that lets them have a home. Phase 2 is `peer_trust`, `attestation` and the verification half of `sign_in`, **which is DF-60's exit and not a coincidence**: that entry asks for everything after the code exchange to become a function taking the fetched document and the `PeerTrust`, with the command reduced to the browser and the socket, and a crate that may not name Tauri is what forces it. Phase 3 is `listener`, `link_invite`, `broadcast_secrets` and `commands.rs` above its first `#[tauri::command]`, **and it was split on its own seam**: the three modules are pure moves and landed together as `WI-M8-004`, while `commands.rs` is cut into three by what each function needs from a `State` rather than by what a file list makes tidy -- the peer half (`WI-M8-005`, landed), the browse half (`WI-M8-006`) and the send half (`WI-M8-007`), in that order because `read_exact` and `read_frame` live in the last of the three. Phase 4 is the second composition root, and **it is the phase with a question in it** -- docs/09 records that receiving is undecided, because a GUI is running when a transfer arrives and a command has exited.

#### Work Items

**Every landed M8 row is in [RECORD.md](RECORD.md): `WI-M8-006` to `WI-M8-015` moved on 2026-09-17 at the ninth ceiling, `WI-M8-017` to `WI-M8-026` on 2026-09-19 at the tenth.** What is left in the table below is what is not closed. The original note follows.** `WI-M8-006` through `WI-M8-015` are closed and their rows are the longest thing in this file; the Review record already carries why each went the way it did, and the rows carry what each one moved. **Moving is what the ceiling asks for and shortening is what it forbids**, so they went across whole. **`WI-M8-016` landed the same day and never had a row here**: it is the three deferred entries DF-78, DF-79 and DF-80 taken together, and the Review record carries what it closed and what it did not. **What it did not close is DF-79's bind half**, which is in Deferred below with the reason it is a design change rather than a test.

| ID | Content | Status | Critical |
|---|---|---|---|
| WI-M8-031 | **The storage ladder stops at a failed rung only when no lower rung answers, to DCR-143.** `select_rung_index` walks past a failing rung remembering the first failure, returns a lower rung's key when one answers, and raises the remembered failure only if the walk ends with nothing found -- so it never falls through to `Ok(0)`, which is the case where the caller would mint over a locked collection. **PASS, no REVISE, and the Implementer's diff was right on the first dispatch.** **Both findings were mine and both were in my own tests**: a seven-line comment I wrote failed `comment-length` (rule A2), and I had missed a second Supervisor test one layer up -- `device_identity.rs`'s `a_rung_that_cannot_be_read_stops_the_search_rather_than_minting_a_key`, whose refusal still holds but whose `low.loads() == 0` asserted the very stop being narrowed. **Section 6 is what caught it**: the tests are the standard, so a standard written in two files fails until both are read. The composition level gained the case this machine is actually in, a locked Secret Service above a file rung that holds the key, asserting the Device ID is the one the earlier run wrote. **Killed by mutation twice**: dropping the remembered failure fails six tests including both safety ones, and reporting the lowest failure instead of the highest fails five. `tradr-identity` 30 ladder tests, `tradr-app` 11; the workspace 1693 -> 1701 | **done** | **yes** |
| WI-M8-032 | **Sign-in survives having no browser, to DCR-143.** `open::that` failing prints the authorization url to stderr and the flow goes on waiting, instead of returning an error naming a listener it had just dropped. The loopback bind takes 127.0.0.1:21821 with an ephemeral fallback, following `bind_with_fallback`'s shape including the refusal it reports, and the `ssh -L` line is composed from the port actually bound rather than from the constant. Both go to stderr, never stdout, because `receive` prints arrival paths there. **PASS after one REVISE, and the finding is the one this Work Item exists to prevent**: the placeholder was `<this host>`, so the line a person selects and pastes is `sh: Syntax error: end of file unexpected` -- `<` is a redirection operator. **A message describing a recovery the reader cannot perform is the defect being repaired, arriving in the repair**; `USER@HOST` pastes. The Implementer's E1 evidence was real and I re-ran all three mutations. **This was also the first dispatch in six to report its own gates**, having not backgrounded them. The workspace goes 1701 -> 1705 | **done** | |
| WI-M8-033 | **A device is not one of its own peers, to DCR-144.** `drain_peer_sources` takes this device's own `DeviceId` and discards an `Observed` whose observation carries it, so self never enters the `PeerList` -- and therefore cannot be listed, resolved or dialled. Both front ends supply the id from the `PublicIdentity` they already hold. `tradr-core` is untouched. **The test that decides it drives a real observation carrying our own id through the drain and asserts the list stays empty**, and a second asserts an observation carrying a different id still arrives | planned | |
| WI-M6-009 | **Scanning a QR with a camera.** `WI-M6-007b` shows a QR and accepts a pasted blob, which is the whole payload either way (docs/11: "one payload and one parser"), so a QR read by any camera application already links. **A scanner inside Tradr is a platform integration and not an interface change** -- Android has `@tauri-apps/plugin-barcode-scanner` and a camera permission to justify, and the desktop has no camera API at all -- so it is its own Work Item rather than a line in one about React | planned | |

## Deferred

Things consciously postponed. **These live here, not in TODO comments in the code.**

**A closed entry moves to [RECORD.md](RECORD.md) rather than being shortened**, which is what this file's ceiling asks for; ten went across on 2026-09-06 when the ceiling was first reached.

| # | Content | When | Source |
|---|---|---|---|
| DF-93 | **`ci/dispatch-implementer.sh` cannot tell a REVISE round that did nothing from one that worked, and it said the wrong one twice today.** The script compares `git status --porcelain` before and after; on a first dispatch that distinguishes an empty run from a real one, and on a **second** dispatch against an already-dirty tree the file list is identical either way, so it printed "tree unchanged: the run produced no work, dispatch it again" over a correctly applied one-line fix. **Following that advice would have dispatched the same order again**, and the second Implementer would have found the finding already fixed. The content of the tree is what changed, not the list -- a hash of `git diff` would separate them, and the script already runs `git status` twice so the cost is one command. **It is a Deferred entry rather than a Work Item because the message is advice and not a gate**: the script still exits non-zero, which is what stopped the run from being believed | Before the next milestone, or the first time it misleads a session that does not check | Review of `WI-M8-028` |
| DF-94 | **[docs/04](docs/04-protocol.md#partial-files) promises that partial files untouched for seven days are swept at startup, and nothing sweeps anything.** Found 2026-09-20 while ruling DF-89, in the same list as the line being repaired. **The document was describing an intention in the present tense**, which is the shape DCR-140 closed on Android four hours earlier and the shape the key-storage table carried for three platforms before that -- three in one day, which is the finding rather than the sweep. DCR-142 marks the line unimplemented rather than building it: **a sweep needs a startup hook, a clock and a retention policy**, and folding an unbuilt feature into a two-line repair is how a repair stops being reviewable. **What it costs while unbuilt is bounded by DCR-142 itself** -- the directory a completed transfer leaves is now removed, so what accumulates is only what an interrupted transfer left, which is the thing a sweep is actually for | With whatever next gives the receive path a startup hook, before M9 | Ruling DF-89, 2026-09-20 |
| DF-97 | **The url a person must open is printed twice in one line, and the first copy is inside a launcher error.** Measured 2026-09-21 on the first headless `tradr-cli receive`: `open::that`'s failure carries the whole authorization url in its own text -- `Launcher "xdg-open" "https://accounts.google.com/..." failed with ExitStatus(...)` -- and DCR-143's line then appends the url again, so the one thing the reader is looking for sits behind about four hundred characters of the thing they cannot act on. **The information is all there, which is why this is Deferred rather than a third REVISE round**, and the Work Item it belongs to shipped working. **It is the same class as the `<this host>` finding that round did catch**: a message whose purpose is that a person can act on it, made unreadable by what was convenient to concatenate. The exit is to print the url on its own line and keep only the launcher's exit status in the failure text | With whatever next touches the desktop sign-in output | First headless receive, 2026-09-21 |
| DF-96 | **The storage ladder is presented as a security gradient and is not one, raised by the user on 2026-09-20 while DCR-143 was being ruled.** Their argument: offering a plaintext `0600` rung at all concedes the at-rest risk the Secret Service rung exists to cover, so the ladder cannot be defended as a security mechanism. **The counter is narrower than it first looks.** A key lives on exactly one rung, so a device whose key is in Secret Service has an empty file rung and loses nothing to a stolen disk -- the benefit is not cancelled. **What is cancelled is intent**: which rung a device lands on is decided by whether a keyring happened to be running at first start, docs/05 forbids promotion afterwards, and so a device that once started without one is plaintext forever with nobody having chosen that. **Two measurements make the top rung narrower still**: a login keyring with an empty password stores every item in `*.keyring` in plaintext, and while the session is unlocked any same-user process can read any secret -- which docs/05 already concedes in the kernel-keyring section. So the protection is conditional, offline-only, and accidental. **The exit the design has never considered is encrypting the file rung with a passphrase**, the shape an ssh private key has: both rungs then protected at rest, headless included, and no GUI anywhere. The other honest exit is to say plainly that the ladder is about availability rather than security. **It is a decision and not a repair**, which is why it is here | Before M9, and it is decision 15 | User, 2026-09-20 |
| DF-95 | **Whether Google's Device Authorization Grant carries a `nonce` into the `id_token` is unmeasured, and it is the only thing between the CLI and an interface with no port forwarding in it.** RFC 8628 prints a short url and a code entered on any device, which is what `gh auth login` and Azure CLI do and is plainly better than DCR-143's `ssh -L`. **[ADR-0003](docs/adr/0003-google-attestation-as-trust-root.md) is what it has to survive**: device keys are bound by carrying `BLAKE3(device_public_keys)` in the `nonce`, and the OIDC device-flow extension does not settle `nonce` support the way the authorization-code flow does. **The failure is loud rather than silent** -- step 4 of [docs/05](docs/05-security.md)'s seven refuses the Attestation -- so the risk is a wasted Work Item and not a broken trust root. **It also needs a client registration of a type this deployment does not have**, a TV and limited-input device client, which is a Google Cloud Console action before any code is written. Measure before cutting anything | Before M9 | Ruling DCR-143, 2026-09-20 |
| DF-92 | **`IPV6_V6ONLY` is unobservable here in two directions, and the Windows job found the second one hours after this entry predicted it would.** Linux defaults the option to 0, so a `[::]` socket is dual-stack whether or not anything asks: deleting `set_only_v6(false)` leaves the behavioural test green, **and it also leaves green the test that asserts the option itself** -- `socket2::SockRef::only_v6()` answers `Ok(false)` from the kernel default. **Measured twice on 2026-09-19, and the second measurement was the Implementer refusing my repair with evidence**, which is the third time in this repository an Implementer has been right about the Supervisor's instructions. **It is a limit rather than a test-design failure**: on a platform whose default already equals the value, "set explicitly" and "inherited" are the same observable state. **What carries it is the `windows` CI job**, which runs `cargo test --workspace`, so `dual_stack_socket_has_v6only_disabled` goes red there and by name rather than through a connection that times out -- which is why that test is kept although it is vacuous here. **This is DF-86's trade one layer up and the opposite answer**: there the exit was a per-target check on the commit path, and here no such check exists, because the option is a runtime value and `cargo check` never evaluates it. **The prediction was tested the same day and held.** `WI-M8-027`'s first Windows run failed -- not on the option being ignored, which works there, but on the option being *refused*: Windows answers `WSAEINVAL` when it is set on a specific address rather than the wildcard, and Linux accepts that call silently. **So Linux swallows both "setting it changes nothing" and "setting it is not allowed"**, which is a wider blindness than this entry first claimed | With whatever first makes a Windows runtime check available on the commit path, before M9 | Review of `WI-M8-027`, 2026-09-19, by mutation |
| DF-85 | **DCR-136's ordering is unobservable, and the test asserting it was mine.** Measured while reviewing `WI-M8-023`: moving the colon replacement from before the reserved-name check to after it leaves all 13 `path_sanitization` tests green, and **396 inputs crossing every Windows reserved name with leading, interior and trailing colons give byte-identical results under both orders.** The reason is arithmetic rather than luck -- a stem holding a colon is never in the reserved list, replacing a colon with `_` never produces a name that is, and `_` occupies the position the colon held so the stem and extension divide at the same index. **The entry is here rather than in the review because the defect was the standard and not the implementation**: I wrote `a_colon_cannot_step_around_the_reserved_name_escape` before the code existed, and it is a test standing where the bug cannot be -- `WI-M7-007e`'s finding in a fifth place. It now asserts what it can, that a reserved name carrying a colon comes out safe, and the ordering claim went out of [docs/04](docs/04-protocol.md#name-collisions-and-sanitization) with it. **What stays open is whether an order nothing can observe should be specified at all.** It is specified because two implementations reading one sentence differently is its own defect, and because the argument above holds only for the reserved list as it stands -- a future entry containing `_` would make the orders differ, and nothing would say so. **A test cannot be written for it today**, which is why this is carried: the exit is a check over `WINDOWS_RESERVED` itself, refusing an entry that is not plain alphanumeric, and that is a rule nobody has asked for yet | With whatever next adds to `WINDOWS_RESERVED`, before M9 | Review of `WI-M8-023`, 2026-09-18, by mutation |
| DF-83 | **`bind_quic_transport`'s two lines survive mutation, and DCR-131 narrowed that residue rather than removing it.** Measured on the delivered tree by the Supervisor: swapping the composition's two arguments -- `bind_with_fallback(key_store, fallback_addr, default_addr)` -- leaves the whole workspace green at 1667 tests, so a device that binds an ephemeral port and never tries 21820 still fails nothing. **That is DF-79's own exposure one layer down and it is smaller in exactly the way the seam was cut to make it smaller**: what survives now is two lines naming two locals in order, where before the seam it was a whole function holding both constants and both binds. **It is untestable here for the reason DF-82's first arm is**: the composition names 21820, which is a port no test owns -- free on a runner, taken on any machine running Tradr, and a test asserting either answer would be asserting the machine's state rather than the code's. **The exit is not a test.** Either the pair reaches the composition from somewhere a test can supply, which is the caller-supplied pair [docs/02](docs/02-architecture.md#two-front-ends-one-device) refuses and DCR-131 refused again, or the constant stops being a constant. **Carried rather than repaired because both exits are the design change this one just declined** | With whatever first makes the default port configurable, before M9 | Review of `WI-M8-018`, 2026-09-18, by mutation |
| DF-81 | **`bundle_dmg.sh` fails intermittently on the pinned `macos-15` runner, and Tauri surfaces none of the script's own output, so the failure names the script and nothing else.** Seen 2026-09-17 on PR #178, `WI-M8-016`: `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace` both passed on that runner, the release profile finished in 6m41s, `tradr.app` bundled -- and ten seconds later `Error failed to bundle project: error running bundle_dmg.sh` ended the job. **A re-run of the same commit passed in 10m55s**, so it is intermittent, which per DF-62 is a datum and not an explanation. **It is not DF-63 and that was checked rather than assumed**: DF-63 is about unpinned `-latest` runners and its remedy is already in `.github/workflows/ci.yml`, which has named `macos-15` since 2026-09-13 -- so this is a pinned image failing, and the class DF-63 left open does not cover it. **What makes it worth an entry rather than a shrug is the second half.** The step that fails is building a DMG, and **DCR-113 dropped the DMG**: decision 7 settled distribution as a direct APK and one Linux package, and M10 records store submission and four package formats as dropped. So `main`'s green depends on packaging a deliverable this project decided not to ship, and a red `main` at a time nobody chose is what that buys. **The cheap exit is the one the decision already implies** -- `cargo tauri build --no-bundle` on the macOS job, which keeps what the job is for (that the tree compiles, tests and links on macOS) and drops what it is not. **The expensive half is whether a macOS build is a deliverable at all**, which decisions 7 and 8 answer for signing and not for the platform, and that is a question for `docs/` rather than for `ci/`. **The trigger fired on 2026-09-18 -- a red `macos` blocked `WI-M8-018`'s merge, by DF-62 rather than by the DMG, which is the cost this entry predicted arriving through the other door.** The cheap half is ruled by DCR-132 and cut as `WI-M8-019`; **the expensive half is what stays here** | The expensive half: with whoever decides whether macOS is a platform this ships to | Found 2026-09-17, CI on PR #178 |
| DF-75 | **Which directory the composition root opens the Device Key from is invisible to every test this workspace can run, and it decides whether a machine has one Device Key or two.** Mutated while reviewing `WI-M8-012`: `crates/tauri-plugin-tradr/src/identity.rs` opening the key from the application data directory instead of `tradr_app::paths::device_keys_dir(&dir)` leaves all 62 plugin tests green, and so does the desktop arm of `crates/tauri-plugin-tradr/src/paths.rs` answering a path in `/tmp` rather than delegating to `tradr_app::paths::app_data_dir()`. **It is DF-64's sentence in a second place**: code that compiles, is wrong, and fails no build, no test and no handshake -- and the failure it permits is the one [docs/02](docs/02-architecture.md#two-front-ends-one-device) names as the reason DCR-124 exists, a second Device Key on one machine reported by nothing. **The exposure is smaller than it was and that is why this is carried rather than repaired**: `dirs::data_dir()` and the string `keys` each now have exactly one home in the tree, both under test, and what survives is a two-line delegation. **What would see it is a test over `tauri::test::mock_app`**, which needs `tauri`'s `test` feature in the plugin's dev-dependencies and is a design addition rather than a line | With whatever first gives the plugin a test that can hold an `AppHandle`, before M9 | Review of `WI-M8-012`, 2026-09-16 |
| DF-76 | **`ci/dispatch-implementer.sh`'s cut-off detector cannot tell `agy` reporting its own cut-off from a log that merely quotes the wording, and it fired that way on the run that repaired it.** Measured on `WI-M8-012`: the Work Order asked the Implementer to make the DF-74 grep fire, the demonstration put both wordings into the log, and the dispatch harness then reported `RUN WAS CUT OFF` on a run that had finished and delivered a complete report. **The old detector did this too**, so DF-74's repair widened a hole rather than opening one. **It is worth an entry rather than a shrug because the consequence is the one DF-73 already recorded**: a detector that cries wolf is one a later Supervisor learns to wave through, and the occasion it is not innocent is the one that matters. **The repair is to anchor on the line `agy` itself writes** rather than on the phrase anywhere in the file -- `WI-M8-011`'s log ends `[agy] print timeout after 30m0s with turn in progress`, and the older wording's prefix is not recorded anywhere, so anchoring needs that wording observed once rather than guessed **A cut-off was next observed on `WI-M8-014`, 2026-09-17, and it was a true positive**: the log is the single line `[agy] print timeout after 30m0s with turn in progress; returning partial output` and nothing else, so no report arrived at all and the Supervisor ran every gate and every mutation here. **That is the newer wording a second time and the older one still unmeasured**, so the anchor this entry asks for can be written for one of the two and not for both. | When a cut-off is next observed, so the older wording's prefix is measured rather than assumed | Review of `WI-M8-012`, 2026-09-16 |
| DF-77 | **`ci/state-sync.sh`'s path check answers differently on a machine that has built and on a fresh checkout, so the pre-commit gate passed a reference CI refused.** Found on the DCR-125 docs commit: a sentence naming the debug build directory passed `sh ci/run-all.sh` here, because this machine had built, and failed the `checks` job, which reported that the referenced path does not exist. **The check is right both times and that is the defect** -- it reads the working tree, and the working tree is not the same object on the two machines. **It is DF-65's sentence with the axis changed**: that entry is about a path that has moved, this is about a path that exists only sometimes, and both come of a check that reads the filesystem rather than git. **The cheap half is a rule rather than a script**: nothing a document says should name a path inside the build output directory, since a build artifact is not a reference -- **and this entry broke its own rule on the commit that introduced it**, which is how the rule got measured twice. **The general half is DF-65's own exit**, a check that asks git what has existed | With DF-65, or the first time a document names a generated path again | Review of `WI-M8-012`, 2026-09-16 |
| DF-71 | **`android-actions/setup-android@v3` installs an SDK package Google has deleted, so the bare `uses:` line that worked for eight months began failing on an unrelated diff.** The action's default `packages` input is `tools platform-tools`; `tools` is gone from the SDK repository, `sdkmanager tools` answers `Failed to find package 'tools'`, and the step exits 1 in under 30 seconds. **Measured rather than assumed to be flaky**: the job passed on the three runs of 2026-09-14 and failed twice on `WI-M8-006`, which touches no Android file, the second time on an explicit re-run. Repaired the same day by pinning `packages: platform-tools` on both usages -- the job's own `sdkmanager --install` step already brings the platforms, build-tools and NDK it actually builds against, so the default was supplying nothing it needed. **The entry stays open because the shape recurs rather than because this instance is unfixed**: an unpinned action's defaults are an external dependency the lockfiles do not cover, and the toolchain paragraph in [RECORD.md](RECORD.md) already argues that a toolchain CI does not pin is a reproducibility hole | 2026-09-15, CI on PR #162 | Supervisor |
| DF-68 | **`drain_peer_sources` reports a `SourceMismatch` rather than discarding it, its comment says so in rule F6's own words, and nothing can make the report happen.** Mutated while reviewing `WI-M8-005`: both `list.apply(..)?` calls replaced by `if list.apply(..).is_err() { continue; }` leave `dial_dispatch` at 12 passed and `static_peer_transfer` at 5 passed. **The cause is the comment's own argument turned around**: each source is applied under its own `SourceId`, so a mismatch can only mean a source produced an event it does not own, and no source in this repository can -- `MdnsSource` and `StaticPeerSource` each emit under one constant. So the `?` compares a value with itself the way DF-67's guard does, and the comment claiming the error is reported is true about the code and unfalsifiable by any test. **It is DF-14's sentence in a third place**, after DF-67, and the family is now large enough to be a pattern rather than three incidents: a check that exists because a field means something, unreachable because nothing can make the two sides differ. **Carried rather than repaired because the code is a verbatim move** and because the alternative -- deleting a rule-F6-compliant `?` -- is worse than an unreachable one. **The exit is whatever first lets a `PeerList` be fed by a source that does not own its `SourceId`**, which is a `tradr-discovery` test seam and not a line here | With whatever first constructs a source that can emit under another's `SourceId`, before M9 | Review of `WI-M8-005`, 2026-09-15 |
| DF-67 | **The `TrustTier::SameAccount` guard in `finish_sign_in` cannot be made false, and the comment beside it says it verifies rather than assumes.** Mutated while reviewing `WI-M8-003`: the whole `if tier != TrustTier::SameAccount { return Err(..) }` deleted leaves all eight tests in `crates/tradr-app/tests/sign_in.rs` green. **The cause is structural rather than a missing test.** `AttestationPolicy::own_account` is built from `claims.iss` and `claims.sub` three lines above, so the account the classification compares against *is* the token's own account; every other thing `classify_with_profile` checks -- signature, audience, nonce binding, staleness -- answers `Err` rather than a different tier. So the guard compares a value with itself and the comment claiming it "verifies rather than assumes" is false. **It is DF-14's sentence in a second place**: a comparison that is there because the field means something, and is unreachable because nothing can make the two sides differ. **The exit is a decision rather than a line**: either the guard goes, and the comment with it, or `own_account` comes from somewhere that is not the token -- and nothing today has such a source, since a device learns its account from exactly this token. Carried rather than repaired because the code is a verbatim move and both mutations the Work Item existed for are dead | Before M9, or whenever a device learns its account from something other than its own token | Review of `WI-M8-003` |
| DF-65 | **`ci/state-sync.sh` cannot tell a path that never existed from a path that has moved, and every phase of the extraction will cost allowlist entries because of it.** Found by `WI-M8-001`: moving five modules broke three sentences in `RECORD.md` that were true on the day they were written, and the check reported all three as `does not exist`. **The check is right to be strict** -- it exists to catch a Supervisor writing a path that is simply wrong -- but `RECORD.md` is append-only history by its own opening paragraph, so a historical path is not a defect and rewriting it would falsify the record. The answer `WI-M8-001` used is the one `ble_probe.rs` already set, an allowlist line per path with a reason, and it is correct and it does not scale: phases 2 to 4 move roughly fifteen more files. **What would see the difference is `git log --diff-filter=D` or a rename lookup** -- a path that once existed in this repository's history is history, and a path that never did is a typo -- and that is a check that reads git rather than the working tree, which nothing in `ci/` does today | With phase 3, or sooner if the allowlist passes ten moved-file entries | Review of `WI-M8-001`, 2026-09-14 |
| DF-64 | **Which platform `provider_profile` selects is invisible to every test this workspace can run, and it decides which OAuth client every device presents.** Mutated while reviewing `WI-M7-015`: the `cfg(target_os = "android")` arm can be made to answer `Platform::Desktop` and nothing notices -- `cargo test --workspace` builds for the host, where that arm is not compiled, and `cargo clippy --target aarch64-linux-android` compiles it but asserts nothing. **It is DF-54's sentence in a new place and a worse one**: DF-54 is about code that does not compile, and this is code that compiles and is wrong. The failure it produces on a device is `TRADR_OAUTH_CLIENT_IDS names no client for 'desktop'` on a string that has one, or a credential call refused by the provider -- neither of which names a `cfg`. **What would see it is a test that reads `platform_label` for both variants**, which exists, plus an assertion that the plugin asks for the variant matching its build, which nothing can express while the selection is a compile-time `let` | M8, with the on-device run | Reviewing WI-M7-015, 2026-09-14 |
| DF-63 | **Every runner in CI is an unpinned `-latest`, and this repository had already learned that lesson for the JDK without generalising it to the OS.** Found 2026-09-14 when `macos` failed on a Markdown-only pull request: `clang` died with `Segmentation fault: 11` while linking `phf_macros`, and cargo reported it as `could not compile phf_macros`, so **a linker crash arrives dressed as a code error**. The image was measured from the job log rather than guessed -- the release tag `macos-26-arm64` at build `20260907.0351`, so `macos-latest` has rolled to macOS 26 -- and the published issues describe objects built for a newer macOS failing to link against an older deployment target. **Five macos runs passed and one failed on that same image today**, so it is intermittent on the new image rather than deterministic, which is the worse shape: it breaks `main` on a green tree at a time nobody chose. `macos-15` is pinned and `macos-14` is deprecated, both read off the runner-images list. **What is left is the class rather than the instance**: `ubuntu-latest` and `windows-latest` carry the same exposure and were not touched, because changing two working runners on one observation is the kind of scope this file exists to refuse -- the Linux jobs in particular are tuned to a WebKitGTK stack whose version the image decides. The rule RECORD.md already states for the JDK is the rule: pin the major version, because a runner that defaults to something newer reproduces the failure exactly and names neither the cause nor a version that would work | When a `-latest` roll breaks a green tree again | [RECORD.md](RECORD.md) |
| DF-62 | **The two QUIC loopback tests in `send_files.rs` fail non-deterministically on a hosted runner, first seen 2026-09-13 on a docs-only pull request, and the evidence that it is not the code is the test binary's own name.** **It is not Windows-only, measured on `WI-M8-011` on 2026-09-16**: `macos` failed `send_files_end_to_end_over_quic_loopback` with the same `failed to read accept frame: transport error: the operation exceeded its deadline` after 30.22 s, on a diff that touches `identity.rs` and two manifest lines and nothing this test reaches, while `rust` passed the same target on the same commit and `cargo test --workspace` passed twice locally. **So the sentence this entry led with was narrower than its own argument**: the reasoning was always that a transport deadline means packets did not arrive and the tree was identical, and nothing in it was about Windows. UDP loopback on any hosted image is the variable, and the entry's remedy and its cost are unchanged. **Seen a third time on 2026-09-14, on `WI-M8-002`, whose whole diff in that file is one `use` line**: `send_files_respects_receiver_item_filtering` panicked with `failed to read accept frame: transport error: the operation exceeded its deadline` after 30.75 s while `send_files_end_to_end_over_quic_loopback` passed in the same binary, and `gh run rerun --failed` was green on the same commit. **A rerun is the whole remedy today and it is not one**: it makes a red Windows job indistinguishable from a real regression, which is the cost this entry is accruing. `cargo test --workspace` on `wi-m7-fingerprint-is-not-the-redirect` timed out both `send_files_end_to_end_over_quic_loopback` and `send_files_respects_receiver_item_filtering` with `transport error: the operation exceeded its deadline` after 31.13 seconds; the same two tests on `wi-m7-013`'s run, whose only difference is three Markdown commits, finished in **0.28 seconds**. **Both runs executed `send_files-a1d028d089dbff9b.exe`** -- an identical metadata hash, so identical compilation inputs -- and re-running the failed job alone on the same commit passed. So the variable is the runner and not the tree: a deadline on a transport means packets did not arrive, and UDP loopback on a hosted Windows image is the only thing that changed. **What this costs is the ability to read a red Windows job**, which is what makes it deferred rather than ignored: the next real Windows regression will look exactly like this, and the answer today is to re-run the job once and compare the two durations. A fix belongs with whatever gives these two tests a bound they choose rather than one `quinn` picks **Sighted a third time on 2026-09-18, on `WI-M8-018`'s `macos` job**, and this time the failing name is the other of the two: `send_files_respects_receiver_item_filtering`, with the same `failed to read accept frame: transport error: the operation exceeded its deadline`, on a diff reaching `crates/tradr-app/src/network.rs` and its test file and nothing that test touches. **The other seven jobs passed on the same commit, `cargo test --workspace` passed here at 1667, and a re-run of the same commit passed** -- so the entry's own reading holds for a third occurrence, and what is new is only that both named tests have now failed at least once. **It cost a merge this time**, which is what this entry said it would eventually do.| When a red Windows job is not explained by a re-run | [docs/03](docs/03-discovery-and-transport.md) |
| DF-61 | **`cargo clippy --target aarch64-linux-android --workspace -- -D warnings` cannot pass on this repository and DF-54's instrument is narrower than the entry implies.** Two errors land in `apps/tradr/src-tauri/src/lib.rs` -- an `unused_mut` on `let mut builder` and an `unused_must_use` on `#[cfg_attr(mobile, tauri::mobile_entry_point)]` -- both inside Tauri's own macro expansion for the mobile entry point, and both reproduced on `HEAD` with the working tree stashed, so they predate everything in flight. **`crates/` is clean under that target and `apps/` is not**, which is why `WI-M7-009`'s round passed: its command had no `--workspace` and so compiled one package. **The entry is that the two forms are not interchangeable and a Work Order that reaches for the wider one fails on someone else's code** -- mine did, and the Implementer reported it rather than repairing a file it was not given. The exit is a decision about the two lints: an `#[allow]` on an expansion this repository does not write is DF-22's refused attribute, and the alternative is naming the excluded package in every cross-target gate | With whatever first makes an Android gate part of the commit path, DF-54's own condition | Review of `WI-M7-012` |
| DF-59 | **The unreadable-secret cause is now printed twice, because two providers each hold their own `last_report`.** `WI-M7-011` builds a second `DeviceBroadcastSecrets` in `lifecycle.rs`, one for `BleSource` and one for `BleRotation`, and each reports a cause when *it* sees the condition change -- so a registry that fails to load writes its line twice rather than once. **One instance cannot be shared as the code stands**: `BleSource::new` and `BleRotation::new` each take a `Box<dyn BroadcastSecrets>` by value, so sharing means `Arc<dyn BroadcastSecrets>` in both signatures, which is a `tradr-discovery` change and its own Work Item. **The duplication is in the reporting and not in what goes on the air**: both instances read the same three stores and answer the same sets. | With the next `tradr-discovery` signature change | WI-M7-011 |
| DF-58 | **`tick.wait()` and the slot constant are indistinguishable to any fixture, so the one line that makes the schedule the rotation's decides nothing a test can see.** Mutated in `BleAdvertising::run` while reviewing `WI-M7-011`: the `Ok` arm's `wait: tick.wait()` replaced by `Duration::from_secs(BLE_ADVERTISEMENT_SLOT_SECS)` left all nine tests green, because `RotationTick::wait()` *is* that constant today and `RotationTick` has no public constructor, so no test can build one answering anything else. **This is not DF-55's shape**: there the test derived its expectation from the constant and could have been written otherwise, here no fixture exists at any price short of a constructor in `tradr-discovery`. **The code is correct and the mutation is what a later variable slot length would make wrong**, which is the moment this becomes testable rather than a note. | When a tick answers a wait that is not the slot | WI-M7-011 |
| DF-57 | **`BLE_ADVERTISEMENT_SLOT_SECS` can be any value at all with every test in `tradr-discovery` green, and it is DF-55's sentence a second time one Work Item later.** Mutated from 4 to 9 while reviewing `WI-M7-010`: `cargo test -p tradr-discovery --test rotation` stayed green, because every expectation in that file derives from the constant and `BLE_ADVERTISED_SET_MAX` derives from it too, so the pair stays self-consistent at any slot length -- the `const _: () = assert!(...)` checks that consistency and not the value. **Writing 4 into the tests would not fix it**: that is a second copy of the constant, which diverges rather than checks. The value is a claim about the air -- roughly two expected reports per slot at `WI-M7-005c`'s measured rate -- and **the only instrument that reads it is a recorded run against real hardware**, which is where DCR-085 already puts every other claim of this kind. | With the two-radio run | WI-M7-010 |
| DF-56 | **Starting Tradr turns on a radio the user turned off, and the design never said whether it may.** `BluerScanner::new` calls `adapter.set_powered(true)` when the adapter is not powered, and `WI-M7-007o` is what made that line reachable: until it landed, nothing constructed a `BluerScanner` outside a test. So a user who switched Bluetooth off and then launched Tradr has Bluetooth on again, silently, with no setting that says so. **It is outward-facing behaviour rather than a bug** -- the alternative is reporting `AdapterUnavailable` and running with no BLE discovery, which is what every other platform's off switch already produces -- and [docs/03](docs/03-discovery-and-transport.md#a-ble-error-says-why-and-one-variant-is-the-retreat) gives `AdapterUnavailable` a variant precisely so that condition can be reported rather than repaired. The exit is a decision, and the Work Item behind it is one line either way | 2026-09-12 | WI-M7-007o review |
| DF-55 | **`BLE_OBSERVATION_TTL_SECS` can be any value at all with every test in two crates green.** Mutated from 30 to 987 654 -- eleven days -- and `tradr-discovery` and `tauri-plugin-tradr` both stayed green, because `crates/tradr-discovery/tests/ble.rs` advances by `BLE_OBSERVATION_TTL_SECS - 1` and `crates/tauri-plugin-tradr/tests/ble_source.rs` advances by `BLE_OBSERVATION_TTL_SECS`, so **both age-out tests compute their expectation out of the very constant they exist to pin**. This is `WI-M7-007k`'s finding measured a fourth time and the third distinct constant it has caught. [docs/03](docs/03-discovery-and-transport.md#lost-is-an-age-out-and-it-is-evaluated-when-a-report-arrives) gives 30 a reason -- several advertisement intervals, far below the 15-minute rotation -- so it is a design decision and a literal on one side is what pins it. **It is `WI-M7-003`'s suite rather than `WI-M7-007o`'s**, and `WI-M7-007o` was forbidden that file, which is why it is here and not a REVISE item | 2026-09-12 | WI-M7-007o review |
| DF-54 | **Nothing this repository can run compiles a `cfg(target_os = "android")` line, and `WI-M7-007m` shipped a broken import through every gate because of it.** `cargo clippy --workspace --all-targets`, `cargo test --workspace` and `ci/run-all.sh` all build for the host, so `spawn_ble_gatt_listener`, `AndroidGattAcceptor`, `GattLinkSink`, `ble_probe.rs` and the Android half of `ble_android.rs` have **never been type-checked by any local gate** -- only by `android-debug-smoke` in CI, which needs an NDK, an SDK and eight minutes. The defect that exposed it was an import path: `tradr_identity::ClockKeyBindingVerifier` where the crate exports `tradr_identity::key_binding::ClockKeyBindingVerifier`, green on four gates and red in CI. **The instrument exists and costs nothing, which is what makes this a Deferred entry rather than a bound**: `cargo check --target aarch64-linux-android --workspace` runs on this machine in under six seconds with no NDK and no SDK, because `cargo check` does not link, and the four `*-linux-android` rustup targets are already installed -- measured 2026-09-12, reproducing the CI failure exactly and then its repair. **What makes it a decision is where it goes.** `ci/run-all.sh` compiles no Rust at all (DF-30 says so and that is why three defects once reached an Implementer), so this is not a check to add beside the other twelve; it belongs with `cargo clippy` and `cargo test` on the pre-commit path, and adding a fourth command there is a change to the gate every commit pays for. **It also does not generalise for free**: the same hole covers `cfg(target_os = "windows")` and `cfg(target_os = "macos")`, which CI does build, and a cross-check per target is a different proposition from one | With the next work in `ci/`, and before `WI-M7-007n` adds more Android-only code | Review of `WI-M7-007m`, round 2 |
| DF-53 | **The three `execute_*` functions declare a capability set nothing checks, and `WI-M7-007m` threaded a parameter through them that can be thrown away with the suite green.** `capabilities` in `execute_send_files_with_progress`, `execute_list_peer_directory` and `execute_download_file` goes straight into `HandshakeParams` three lines later; replaced with `Capabilities::empty()` at all three, `cargo test -p tauri-plugin-tradr` stays entirely green -- measured while reviewing `WI-M7-007m`. **It is not a regression and the parameter is not the cause**: the literal `Capabilities::DIRECT_QUIC` that stood there before was equally unchecked, so the Work Item made an uncovered line no worse and no better. **The reason it is uncovered is structural and already named**: `Session` answers `peer`, `tier`, `negotiated_version` and `peer_max_frame_size` and does not report what the peer declared, so the receiving side of an end-to-end test cannot see the capability set that arrived -- the listener's own half is pinned only because `listen_for_transfers_reads_capabilities_fresh_per_connection` reads the raw `Hello` frame instead of completing a handshake. **The exit is a decision rather than a line**: either `Session` reports the peer's declared capabilities, which is the first thing that will ever read them anyway, or every test that wants to check one decodes the frame by hand the way that one does. DF-35's sentence in a fourth place | With whatever first reads a peer's declared capabilities, before M8's Brokr work | Review of `WI-M7-007m` |
| DF-52 | **`PeerExpectation::Identity`'s second clause is unreachable through a `SecureChannel` and `ble-gatt` is the first transport to notice.** [docs/03](docs/03-discovery-and-transport.md#what-a-transport-is-told-about-the-peer-it-is-dialling) says that variant refuses unless the agreement key the channel authenticated is the one named, and `SecureChannel` answers a `DeviceId` and nothing else -- so `BleGattTransport::connect` compares the `DeviceId` and cannot compare the key. **It is not a hole in what `ble-gatt` authenticates**: `Noise_XX`'s identity join is verified by `KeyBindingVerifier`, which is handed the peer's agreement key and refuses a binding that does not cover it, so the key is checked during the handshake and merely not re-compared afterwards. **What is genuinely unreached is a device that rotated its agreement key under an unchanged identity key** and a caller holding the old `PublicIdentity`. `direct-quic` has the same gap and nothing has ever constructed an `Identity` expectation: every dial in this repository is `Unpinned` or `Device`. The exit is a decision rather than a line -- either `SecureChannel` reports the key it authenticated, which is a `tradr-core` trait change Change Drill D10 forbids doing casually, or the variant says only what it can enforce | With whatever first constructs a `PeerExpectation::Identity` | Review of `WI-M7-007k` |
| DF-82 | **Both arms `WI-M8-017` added to `QuicIncoming::accept` survive mutation, and after DCR-130 one of them carries more than it did before.** Measured on the delivered tree: `ok_or(TransportError::Closed)` turned into `ok_or(TransportError::Rejected)` leaves the whole workspace green, and the `QuicChannel::new` discard turned back into a `?` does too. **The two are not the same defect.** The first is real and untestable here: nothing in this workspace can close a `quinn::Endpoint`, because neither `QuicTransport` nor `QuicIncoming` exposes an operation that does, and **DCR-130 makes that line the only error `accept` can now return** -- so it alone separates "the listening ended" from "one peer failed", and a wrong variant there makes `tradr receive` exit non-zero on an orderly shutdown. The rule it encodes is tested one layer up, through `MockIncoming` in `crates/tradr-app/tests/listener.rs`; what is untested is QUIC's own mapping onto it. **The second is unreachable in production and not only in a test**, which is DF-14's sentence in a fourth place after DF-67 and DF-68: `verify_client_cert` in `crates/tradr-transport/src/tls/verifier.rs` already runs `identity_point` over the peer's certificate, so a certificate reaching `QuicChannel::new` on the listening side has been parsed once already and cannot fail the second parse. The arm is correct by the rule and fires for nobody. **Carried rather than repaired because both exits are design additions**: the first needs a close operation on the listening half, which is API nothing else asks for, and the second needs a dialler this repository cannot write -- Change Drill D3 keeps `quinn` out of every test file | With whatever first gives the listening half a close operation, before M9 | Review of `WI-M8-017`, 2026-09-18, by mutation |
| DF-51 | **Rule F6's instrument does not see an empty block, and `WI-M7-007l` round 1 delivered one.** `if x.is_err() { /* a comment */ }` is a swallowed error by the rule's own words -- it is `catch {}` written in Rust -- and `ci/discarded-result.sh` looks for `let _ =` while `ci/empty-catch.sh` reads Kotlin, so it passed both and reached review. **It is DF-47's shape a second time**: an instrument that finds its candidate by pattern rather than by structure, and the pattern is one an ordinary repair walks straight past. Whether a check can distinguish an empty block from a block the author meant to leave empty is the open question, and it is why this is a finding rather than a Work Item | M8 | Found 2026-09-10 reviewing `WI-M7-007l` |
| DF-48 | **A prober that connects to a `ble-gatt` peripheral learns its permanent keys, and no pattern this design could pick would prevent it.** `Noise_XX` reveals the responder's static agreement key in message 2, encrypted under an `ee` any prober can compute, and [ADR-0020](docs/adr/0020-noise-xx-for-ble-gatt.md) puts the identity join beside it, so a `DeviceId` follows. **It breaks no promise this design made**: [docs/03](docs/03-discovery-and-transport.md#2-ble--proximity-no-network-required-tier-0) says why no permanent identifier goes on the air and the threat it names is a receiver -- shop receivers and passing phones, listening -- and a prober that completes a handshake is not listening. **The exit exists and is cheap**: message 1 carries a MAC over the initiator's ephemeral key under the ABK or Link Secret that matched the EID, and a responder that cannot match it drops the connection before message 2. It forbids nothing this design can do, because [docs/03](docs/03-discovery-and-transport.md#what-a-scanner-reports-and-what-blesource-does-with-it) already refuses to observe an unmatched EID, so no `ble-gatt` candidate for a stranger exists to be dialled. **Deliberately not built here**: it is a new wire concept, and `peer` was the question the cut opened | Before M9's security review | Cutting WI-M7-007b |
| DF-47 | **`ci/empty-catch.sh` finds its candidate on one line, so a `catch` whose parenthesised declaration is wrapped is not a catch to it and an empty one is not refused.** Written into `ci/README.md` rather than left implicit, which is the half DF-26 says the same script family got wrong before. **It is a bound and not a bug**: no formatter in this workspace produces that shape, ktlint is not in this build at all, and reaching it takes a deliberate hand -- the same argument DF-26 makes for its own line-at-a-time miss. **The exit is the same machinery too**, a statement-region accumulator rather than a line, which is what `plugin-permissions.sh` rule 3 already has and what would close both entries at once. Deliberately not fixed here: widening the candidate rule is a decision, and `WI-M7-008`'s Definition of Done did not carry one | With whatever resolves DF-26, before M9's security review | Review of WI-M7-008 |
| DF-46 | **The BLE scanner cannot tell a peer that walked away from a bus that is failing, and answers both by continuing.** `next_report` matches `Err(_) => continue` on `Adapter::device` and on `Device::service_data`, which the Work Order specified and which is right for the ordinary case: a device that vanishes between the event that named it and the read of its properties is routine, and printing on every occurrence would be noise on a busy radio. **What it cannot express is the other case** -- a D-Bus connection that has gone bad makes every property read fail, and the loop then spins through events producing nothing, with no error reaching a caller and nothing said anywhere. It is rule F6's shape in a place `ci/discarded-result.sh` cannot see, since the check reads `let _ =` bindings and this is a match arm. **The exit is a decision rather than a line**: what a scanner owes a caller when the transport under it is failing rather than when one peer is gone, which is a question all four platform implementations share and none of them has been asked | With the second platform implementation, `WI-M7-005` | Review of WI-M7-004 |
| DF-45 | **Change Drill D4's retreat is keyed to a value the first real failure does not produce, measured 2026-09-06.** docs/03 says `Unsupported` is the variant D4 reads -- a platform that cannot advertise reports it once and the device runs scan-only from then on. **On this machine, which genuinely cannot advertise, `BluerAdvertiser::start` returns `Io(Other)`**, because BlueZ answers `Failed` and DCR-085 forbids mapping `Failed` onto `Unsupported` for a good reason: a busy adapter would then retreat a device permanently. **Both halves are correct and the retreat still never fires**, which is the entry. The gap is that "cannot perform this role" is a conclusion about a platform and `Unsupported` is a report about one call, and nothing yet turns the second into the first. **The exit is a decision about what evidence retreats a device** -- a probe at startup, or a count of consecutive failures, or a capability the platform is asked rather than inferred -- and it is a decision all four implementations share, so it is not `WI-M7-004`'s to take | With whatever first consumes a `BleAdvertiser`, before M7 closes | Review of WI-M7-004 |
| DF-44 | **`BleSource`'s age-out is driven by scan reports, so a radio that goes completely quiet ages nothing out.** DCR-084 decides `Lost` on a 30-second monotonic deadline and evaluates it whenever a report arrives, which covers every case in which any Tradr device is in range -- each advertisement from any of them drives the check for all of them. **The uncovered case is the last peer walking away with nothing else nearby**: its observation stays listed until something else is heard. It is bounded staleness in a list a person is reading, not a path anything dials, which is why it is an entry rather than a `REVISE`. **The exit is a capability Layer 1 does not have**: `Clock` reads wall-clock and monotonic time and cannot wait for either, so a timer belongs behind a port of its own, declared where `BleScanner` is and implemented in Layer 3 -- and adding one is a wider decision than a BLE age-out, since nothing else in this repository has needed to wait on time it did not already own | With whatever first needs to wait on time rather than read it, before M7 closes | DCR-084 |
| DF-43 | **The link stream's drain is unbounded, and the listener that waits on it serves one connection at a time.** `WI-M6-011` gives the inviter the finish-and-drain that makes `LinkApprove` arrive, and the read it ends on has no bound: a replier that never closes leaves the inviter parked. **`listen_for_transfers` awaits `handle_incoming_channel` inline rather than spawning**, so that park stops the device accepting anything at all -- and the same is already true of the human pause inside the exchange, which holds the listener for as long as a person takes to read twelve words. **The transfer branch has carried the identical unbounded read since M1**, so this is one shape in two places and not a regression. Bounding the drain and spawning per channel are one Work Item, not a line in this one | Before M7 | Review of WI-M6-011 |
| DF-42 | **`ci/discarded-result.sh` reads text, so two shapes escape it, and both were measured rather than reasoned about.** A discard renamed to `let _unused = ...` is not reported, and it cannot be: `sign_in.rs` holds a deliberately named `_guard` on the same shape, so refusing `_`-prefixed bindings outright would refuse a legitimate one. And a genuine discard written as a chain whose last line is `.ok();` is skipped, because that is the same line shape a bound multi-line chain ends with -- the false positive that cost this Work Item its REVISE round, traded here for a miss. **The type-aware instrument that would catch both is `clippy::let_underscore_must_use` and DCR-080 records why it cannot be turned on**: eight of its twenty-five hits are `#[tauri::command]`'s own expansion. The escape reopens if that macro stops generating the shape, or if the lint learns to skip an external macro | Undecided | Review of WI-M5-005 |
| DF-41 | **A required status check names a CI job by string, and nothing in this repository can read that string.** `WI-M6-010` deleted the `web` job and `main`'s ruleset went on requiring it, so PR #99 was `MERGEABLE` with every live job green and `BLOCKED` permanently -- found only by trying to merge. **The failure is silent in the direction that matters**: the gate did not go red, it stopped being able to report, and `ci/run-all.sh` cannot notice because the ruleset lives on GitHub rather than in the tree. **The check that fits compares `gh api repos/prokosna/tradr/rulesets`'s required contexts against the job names in `.github/workflows/ci.yml`**, in both directions -- a required check with no job, and, more quietly, a job nobody requires. **It would be the first check in `ci/` needing network access and a token**, which is what makes it a decision rather than a script: every existing check runs offline from a clone, and `ci/run-all.sh` is on the commit path, where a token prompt or a rate limit would turn a gate into an obstacle. The likely shape is a check that skips cleanly with no `gh` credentials **and says so**, run in CI where the token exists, which is the one place this repository has so far refused to let a check skip | With the next work in `ci/`, before M9 | Reconciliation of 2026-09-04 |
| DF-40 | **`WI-M6-010` did not close this and its own measurement is why.** The frontend gate that landed on 2026-09-04 runs `pnpm lint`, `pnpm typecheck` and `pnpm format:check` -- **the exact three commands this entry already recorded as staying green with the violation in place.** A gate for the standard is not a runner for the behaviour, and the two are being confused here on purpose so they are not confused later. **The linking interface is reachable from no test, and the cause is not DF-35's.** `pnpm test` is `echo 'no test runner configured yet' && exit 0`, so **there is no TypeScript test runner in this workspace at all** -- not a missing case, an absent instrument. `apps/tradr/src/Linking.tsx` decides three things nothing can check: that the QR is not rendered above 2953 characters, that the displayed invite is cleared when a proposal arrives, and that an approval refreshes the Link list. **The first was measured here rather than tested** -- `qrcode.react` 4.2.0 renders 2953 base64url characters at 177x177 modules and raises `RangeError: Data too long` at 2954, and a component that throws during render takes the tree down -- so the boundary is known to matter and known to be unguarded. **DF-35 is a `#[tauri::command]` whose `State` extractors nothing can construct; this is a language with no runner**, and the exits are different: that one is a seam, this one is a dependency and a script  **`WI-M6-007c` measured what that costs and the number is the entry now.** Its load-bearing line is DCR-078's -- the reply must carry the blob the pause previewed and never the textarea's current value -- and **changing it to `blobText` leaves `pnpm lint`, `pnpm typecheck` and `biome format` all green**, once one incidental hook-dependency warning is answered the way any developer would answer it. **The warning is the sharp part**: Biome's `useExhaustiveDependencies` fired, about a stale closure and not about consent, and adding `blobText` to the deps list -- the fix Biome itself suggests -- makes every gate pass with the violation in place. **Deleting the other half of DCR-078 is caught by nothing at all**: the reset that discards a preview when the pasted text changes, removed, leaves lint, tsc and the formatter green with no warning of any kind. So the two lines this Work Item exists to hold are guarded by one accidental lint rule and by nothing. **That is a stronger claim than "there is no runner"**, because it names what a runner would have to assert.| With `WI-M6-008`, which is the first thing that drives this interface end to end | Review of WI-M6-007b |
| DF-38 | **`fingerprint_verified` is a field nothing can ever make true, and docs/11 says the exchange writes it.** `Link::new` leaves it `false` and `serve_link_reply` and `send_link_reply` both keep that default deliberately; `LinkRegistry::set_fingerprint_verified` exists and no caller anywhere reaches it. **So it is DF-16's shape exactly** -- a field that looks like state being kept -- and the reason it was not simply written from the approval is a real question rather than an omission: docs/11's Tier 2 flow has both sides "press for verification" as an act of its own, so treating a press meaning "yes, link" as a press meaning "the twelve words matched" would make the record assert something nobody performed. **`WI-M6-007b` therefore does not display it**, because a permanently-false field in a list is worse than an absent one. The exit is one decision -- whether the interface offers a second, separate act -- and docs/05's changed-fingerprint refusal is the first thing that will read the answer | With whatever first reads it, before M9's security review | DCR-077 |
| DF-36 | **The QR stops being the pin and nothing fails.** `PeerExpectation::Device(inviter_device_id)` changed to `Unpinned` in `reply_to_link_invite` leaves `cargo test --workspace` entirely green -- measured. **This is DF-35's sentence a third time and the cause has not changed**: the line sits inside a `#[tauri::command]` taking eight `State<'_, T>` extractors, so nothing in `crates/` can drive it. **What keeps it Deferred rather than a `REVISE` was measured rather than argued**: `verify_link`'s key join refuses a channel whose authenticated `DeviceId` is not `BLAKE3(invite.identity_pub)[0..16]`, so removing the pin costs a failed connection and no credential, and the test that proves it is the one the review's first mutation confirmed. **The exit is the same seam DF-35 names**, one layer out: the dial is the only part of `reply_to_link_invite` that is not already delegated to a testable core, and a helper taking the peer list and the transport would be drivable exactly as `execute_send_link_reply` now is | With `WI-M6-008`, which is what measures a transfer at `TrustTier::Linked` and therefore the first thing that would notice | Review of WI-M6-006f |
| DF-35 | **The rung `identity.rs` keeps is decided by a line no test reaches, and reverting it fails nothing.** `open_identity` selects a rung and now retains it, and `Arc::clone(&ladder[index])` changed to `Arc::clone(&ladder[0])` leaves `cargo test --workspace` entirely green -- measured, not assumed. **The cause is structural rather than an omission**: `open_identity` takes a Tauri `AppHandle`, so nothing in `crates/` can drive it, and the same sentence is already recorded one Work Item earlier, where reverting all five `linked_accounts` call sites to `&[]` also failed nothing. **The walk itself is covered and covered well** -- `select_rung_index` returning `0` regardless fails four tests in `crates/tradr-identity/tests/storage_ladder.rs`, two of them pre-existing, which is also what proves `select_rung` is not a second implementation of it. What is uncovered is the one line that carries the answer from the walk to the thing that keeps it. **The exit is a seam rather than a test**: the ladder build and the selection have no reason to take an `AppHandle` at all, and a function taking the directory and returning the opened trio is drivable from a test with a `tempfile` directory, which is how `FileStore`'s own tests already run | With `WI-M6-008`, moved twice. **`WI-M6-006f` did not close it either, and it added a second instance of the same sentence** -- see DF-36, where reverting the dial's `PeerExpectation` also fails nothing. Three Work Items running have now ended with one uncovered line each and the same cause: a `#[tauri::command]`'s `State<'_, T>` extractors are not constructible from a test, so whatever is decided inside one is decided where no test can look. **That is a seam and no longer an accident.** **`WI-M6-006e` is the first caller of `secret_store()` and did not close this**: the rung reaches `LinkService` as a `LinkServiceParts` field, so the *service* is drivable from a test against a memory-backed store -- which is the shape this entry asks for, arrived at one layer up by a Work Item that was not trying to. **What is still undrivable is `open_identity` itself**, where the selection is made, for exactly the stated reason: it takes an `AppHandle`. The first thing that can observe a wrong rung from a person is `WI-M6-006f`'s approve command **`WI-M6-007a` is the fourth and fifth instance, and it is also the first measurement of what the seam actually costs once a Work Order forbids it.** That Work Order required `preview_link_invite`'s body to be a thin wrapper over a plain `invite_preview` function, and three mutations of that core each failed exactly the test naming them: a Fingerprint taken over the agreement key twice, a skew allowance the core ignores, and an `expired` that is never true. **What still fails nothing is only the wiring** -- `preview_link_invite` handing the core `0` instead of `FUTURE_SKEW_LIMIT_SECS` leaves the workspace green -- so the decision moved out of the command and the argument did not. That is a strictly smaller uncovered surface than DF-36's, and it is the shape this entry asks for. **`remove_link` is the fifth instance and the sharper one**: gutted to `Ok(())`, removing neither the record nor the Link Secret, the whole workspace stays green. Nothing in it can be delegated to a core, because it is three calls into code `crates/tradr-identity/tests/link_registry.rs` already covers -- **and "removal takes effect locally at once" is M6's own completion criterion**, decided in a body no test reaches. `WI-M6-008` is what measures it. | Review of WI-M6-006d |
| DF-32 | **A Secret Service prompt has no bound, and nothing in this repository put it there.** `secret_service` 5.1.0 runs `exec_prompt_blocking` whenever the daemon answers a create or a delete with a prompt path other than `/`, and that function ends in `receive_completed_iter.next().unwrap()` -- **no timeout, and an `unwrap` inside the dependency.** DCR-034 measured exactly this shape once already: a prompt with nobody to answer it "does not fail, it waits", observed as a process that never returned from its own startup. **The collection-level `ensure_unlocked` does not close it**, which is the true half of `WI-M6-003b`'s deleted comment -- that check answers a locked collection and says nothing about a daemon that decides to prompt anyway. It has shipped in `store` since `WI-M0-007e` and is not new; `remove` joins it. **The exit is a bounded call rather than a check** -- either the operation runs somewhere it can be abandoned, or the crate is asked for a non-prompting path -- and the reason it is deferred is that no configuration reachable here has produced the prompt branch yet | Before M9's security review, and before any key-store call moves onto a startup path that has no window yet | Review of WI-M6-003b |
| DF-31 | **`LinkRegistry::persist` follows the file rung's atomic-write pattern halfway, and the half it dropped is the half that recovers.** `crates/tradr-identity/src/link.rs` writes a temp file and renames it, and **leaves that temp file behind when either step fails** -- measured: `a_removal_whose_record_cannot_be_written...` needs a `remove_dir_all` to clean up after itself. `temp_path_for` also names the file from the pid alone, so a stale file from a crashed run whose pid has been recycled is silently overwritten. **`FileStore` handles both and is the precedent DCR-070's own text cites**: `create_fresh_temp_file` uses `create_new` with a counter so the name is provably fresh, and `fail_and_cleanup` removes the temp and reports both errors rather than the first (rule F6). **This is DF-29 read from the other side** -- that entry says the pattern is duplicated byte-identically across two crates and cannot be shared under rule 4; this one says the second copy is not actually identical, and the divergence is already there. Landed in `WI-M6-003`, untouched by `WI-M6-003b` | With whatever resolves DF-29, before a third registry | Review of WI-M6-003b |
| DF-30 | **A Supervisor test file is dispatched without ever having been run through the gates it will be judged by, and `WI-M6-003`'s failed one.** `crates/tradr-identity/tests/link_registry.rs` was not rustfmt-clean, and `.githooks/pre-commit` runs `cargo fmt --check` over the whole workspace -- **so the file protecting the Work Item would have refused the commit landing it.** The hash that stops an Implementer editing the file also stops it fixing the file, which is correct and is what makes this the Supervisor's to catch. **This is the second instance, after `WI-M5-002`'s six-line module doc breached rule A2 in the file written to enforce the rules**, so it is a pattern and not an accident. The instrument is one command before dispatch -- `rustfmt` the file and run `sh ci/run-all.sh` -- and the entry exists because a resolution to remember is what failed the first time. **Run for the first time before dispatching `WI-M7-001`, and it caught one**: `crates/tradr-discovery/tests/eid.rs` opened with an eight-line `//!` run, which `ci/comment-length.sh` refuses under rule A2 -- the third instance of a Supervisor test file breaching the rules it exists to enforce, and the first caught before dispatch rather than by the Implementer or by a refused commit. **The entry stays open because it is a practice and not a fix**: nothing makes the command run, and the next Critical Module Work Item is where that matters again. **That next one was `WI-M7-007a`, the command ran, and it caught nothing because it cannot** -- three defects went out in the dispatch and the Implementer found all three. `rustfmt` was run at edition 2021 against an edition-2024 workspace, so running it *was* the defect; an import named two private `tradr-core` modules; and an import left behind by a cut test was a clippy `-D warnings` failure. **`ci/run-all.sh` compiles no Rust at all**, so none of the three is reachable from the practice this entry records. **The instrument is `cargo test --workspace`, run before dispatch, reading past the unresolved import for the module the Work Item is about** -- a private-module path, an unformatted file and an unused import are different errors from "the module has not been written yet", and telling them apart is the whole of the check | Standing, before every Critical Module dispatch | Review of WI-M6-003 |
| DF-29 | **The atomic-write registry pattern is now duplicated byte-identically across two crates, and the layer rules make it unsharable.** `temp_path_for`, the prospective-then-persist-then-commit order and the separate on-disk record type appear in `crates/tradr-discovery/src/static_peer.rs` and `crates/tradr-identity/src/link.rs` in the same shape, comments included. **Unlike DF-27 this is structural rather than lazy**: `ci/layer-deps.sh` rule 4 lets an implementation crate depend internally only on `tradr-core` and `tradr-proto`, `tradr-core` may depend on nothing (B1) and must need no real filesystem (I4, B5), so a helper that calls `fs::rename` has no legal home in this dependency graph. **The exit is a decision and not a refactor** -- either a small Layer 3 crate both may depend on, which means widening rule 4 and saying why, or accepting the duplication and recording that it is deliberate. A third copy is what makes it urgent, and a third registry is plausible | Before a third registry is written | Review of WI-M6-003 |
| DF-28 | **Three fields of docs/11's Link record are left out rather than landed empty** (DCR-069). `peer_email` has no source at all -- `VerifiedClaims` carries no `email` claim and no linking message carries one -- and `policy` and `known_devices` are read by nothing. **`ProviderProfile::renewal` is the precedent and DF-16 is its entry**: a field nothing writes is worse than an absent one, because it looks like state that is being kept. `policy` additionally waits on open decision 9, which has not settled whether a same-account transfer auto-accepts, let alone a linked one | With whatever first reads one of them | DCR-069 |
| DF-27 | **Layer 0 now parses hex in two places with byte-identical code, comment included.** `link.rs`'s `hex_byte` and the three-line note about `from_str_radix` accepting a leading sign are a verbatim copy of `device_id.rs`'s, because a shared helper would have meant editing `device_id.rs`, which `WI-M6-002`'s Definition of Done did not name (rule F3). **It is duplication of the kind that diverges rather than the kind that stays put**: the sign trap is a real one and a future third copy is likelier to be written from memory than from this one. `share_id.rs` parses a UUID and does not share the shape, so the consolidation is two types wide, not three | With the next work touching Layer 0's identifier types | Review of WI-M6-002 |
| DF-26 | **`ci/plugin-permissions.sh`'s dynamic-import refusal is scoped to one line, and a hand-wrapped one slips past it.** Found by probe while reviewing `WI-M5-006`, after the refusal itself was added: `await import("@tauri-apps/plugin-fs")` is refused, and the same call written with its argument on its own line exits 0. **The bound is honest rather than hidden** -- the check reads a line at a time, so a construct split across lines is neither parsed nor refused, and the README's "what it cannot parse is refused" overclaims by that much. It is Deferred rather than a third `REVISE` because no formatter produces that shape: the whole call is well under any line width, so wrapping it takes a deliberate hand. **The fix is to accumulate a dynamic import the way a static one is accumulated**, which is the same statement-region machinery rule 3 already has | With the next work in `ci/`, before M9's security review | Review of WI-M5-006 |
| DF-23 | **A skipped frame costs nothing to send and the receiver will skip them forever.** `WI-M1-021` made `Classification::Ignorable` skip and read the next frame, which is what [docs/04](docs/04-protocol.md#what-unknown-message-types-are-ignored-actually-covers) specifies -- and neither side bounds how many times in a row it will do so, so an authenticated peer holds a transfer open indefinitely with `0x24` frames. **This is the design's own rule implemented faithfully, not an implementation liberty**, so bounding it is a Supervisor's decision and not a `REVISE`: docs/04 says what is skipped and is silent on how often. It bites only a peer that already cleared a Trust Tier, which is why it is deferred rather than cut | Before M9's security review | [docs/04](docs/04-protocol.md#what-unknown-message-types-are-ignored-actually-covers) |
| DF-22 | **Nothing stops a second `#![allow(...)]` from appearing.** DF-20 is legitimate and recorded, but the only thing that will remove it is a Supervisor remembering to look. `ci/excuse-grep.sh` already greps source for prose that papers over a design problem, and an `allow` attribute is the same act performed in the language rather than in a comment. **An allowlist-backed check over `#[allow]` and `#![allow]` would make DF-20 self-closing**, and every later one visible | After WI-M1-004d closes DF-20 | Review of WI-M1-004c |
| DF-1 | Desktop drag-out, pulling a peer's file into a file manager. A download button substitutes | After M9 | [docs/08](docs/08-platform-integration.md) |
| DF-2 | Shell integration: Windows context menu, macOS share menu, Linux `.desktop` | Phase 3 | [docs/08](docs/08-platform-integration.md) |
| DF-3 | Post-quantum migration. Write an ADR once `rustls` X25519MLKEM768 and hybrid Noise are both stable | Undecided | [docs/05](docs/05-security.md) |
| DF-4 | Android 14+ `ChooserAction` custom actions. Sharing Shortcuts suffice for v1 | Undecided | [docs/08](docs/08-platform-integration.md) |
| DF-16 | **`ProviderProfile` still has no `renewal` field**, the last one docs/05's table lists. It carries real weight -- docs/05's 24-hour silent renewal assumes a refresh token and a `prompt=none` path, and a provider offering neither changes that account's whole revocation story. Left out of WI-M0-008a deliberately rather than landing unused (rule F3), since nothing reads it until renewal exists | With the silent-renewal work | WI-M0-008a, DCR-027 |
| DF-14 | **`Jwk.algorithm` is compared but the comparison is unreachable.** `SignatureAlgorithm` has one variant, so `key.algorithm != algorithm` is always false and the branch is clean. It is there because a JWKS entry publishes what a key is *for*, and RS256 and PS256 can share one RSA modulus, so nothing but that field would stop a key published for one being used for the other. **It becomes testable the day a second algorithm lands, and a test must arrive with it.** Recorded rather than covered by a test that would only appear to exercise it | With the second `SignatureAlgorithm` | WI-M0-011b |
| DF-13 | **`VerifiedClaims` names its contract but does not enforce it.** WI-M0-011's Work Order claimed "a caller cannot build one without having gone through step 2", and **that is false as built**: the fields are public, so anything can construct one. The Implementer said as much. Enforcing it means private fields with a `pub(crate)` constructor, which the 22 tests cannot use from `tests/`; the fix is to move them to unit tests inside the module when WI-M0-011b puts the real constructor in the same crate | With WI-M0-011b | WI-M0-011 |
| DF-12 | **A candidate address may carry a bidirectional override; a `RelPath` may not.** DCR-013 rejects `U+202A`-`U+202E` and the isolates in a filename, because a name is shown at an accept-or-decline prompt and `report\u{202E}fdp.exe` renders as `reportexe.pdf`. DCR-017 gives a candidate address only the `item_id` rules, empty and control characters, so `\u{202E}evil.example:443` is accepted — confirmed by probe. The asymmetry is deliberate today: path selection is automatic and no user approves a candidate, so there is no prompt to spoof. **It stops being deliberate the moment the UI shows a peer's address**, which device details plausibly will | When the UI displays a peer address, M4 at the latest | WI-M0-006g |
| DF-17 | **`serve_one_callback` still cannot bound its own `accept()`.** WI-M0-014b bounds the wait from the composition root, by timing out and then connecting to its own port to wake the parked call. That works and was measured, but the bound lives at the call site rather than in the function whose contract it is, so every future caller has to know to build it again. Moving it inside means changing a Critical Module with 22 Supervisor-written tests, which needs its tests written first | With the next work touching `tradr-oidc` | WI-M0-014b |
| DF-9 | **`KeyStore` is synchronous and two of its operations block.** An Android Keystore `sign` is an IPC to `keystore2`, and `agree` runs once per Noise connection; both occupy a runtime worker for milliseconds. [ADR-0013](docs/adr/0013-layer-1-async-traits-return-boxed-futures.md) left `KeyStore` sync rather than reopening [ADR-0011](docs/adr/0011-keystore-exposes-operations.md) with no implementation to measure against. Resolving it means either `BoxFuture` on `KeyStore` too, or `spawn_blocking` at every Layer 3 call site | Before M1's Noise work | ADR-0013 |
| DF-7 | **`SharedSecret` is not zeroized on drop.** It cannot be, in `tradr-core`: `zeroize` is a dependency the crate may not have, and a hand-written `Drop` needs `write_volatile`, which needs `unsafe`, which `#![forbid(unsafe_code)]` rules out. An ECDH secret therefore lingers in freed memory until overwritten. Resolving it means either letting a Layer 3 type own the zeroizing and having `KeyStore::agree` return something it implements, or accepting the exposure and saying so | Before M1's Noise work | WI-M0-006c |
| DF-6 | `useExactTypes=false` in `buf.gen.yaml`. ts-proto's `Exact<DeepPartial<T>, I>` signature does not resolve under TypeScript 7.0.2, so `fromPartial` and `create` lose excess-property checking. Revisit when ts-proto supports TypeScript 7 | Undecided | WI-M0-001c |
| DF-8 | **The Android WebView renders under the status bar.** The `<h1>` in WI-M0-004b's screenshot sits on top of the system clock: no `viewport-fit=cover`, no `env(safe-area-inset-*)`, no edge-to-edge handling anywhere. Harmless at one heading and wrong for every screen after it | Before M2's Android integration | WI-M0-004b |

---

## Risk status

From [docs/09](docs/09-roadmap-and-risks.md). Update as implementation proceeds.

| # | Risk | Likelihood | Status |
|---|---|---|---|
| R1 | BLE peripheral role means four separate implementations | High | **Measured 2026-09-06, and the first connectivity check found the risk realised on this machine.** `hci0` scans perfectly -- 139 per-advertisement reports in twelve seconds -- and **refuses every advertisement including an empty one**, at the kernel's MGMT layer, below BlueZ. So Linux is scan-only here, Change Drill D4's retreat is live rather than hypothetical, and M7's completion criterion needs Android as the peripheral end. Windows and macOS are still unmeasured |
| R2 | Tauri 2's Android maturity | **Low** | **Two of three conditions met, and the third is the only one left.** WI-M0-004 built a correctly signed APK with all four ABIs and WI-M0-004b ran it; WI-M0-005 proved both call directions and WI-M0-005b proved `ACTION_SEND` on both launch paths, the two conditions most likely to have failed. **Only condition one remains, and it is not about capability**: the build works here, and "reliably" is what CI measures, so it stays unchecked until CI has run it |
| R8 | Brokr-free operation breaks as features are added | High | Make CI's `no-brokr` job required from M1 |
| R9 | macOS and Windows signing procurement takes weeks | Medium | Needs starting before M4. Re-check at the start of M2 |
| R11 | The AppImage bundler downloads unpinned executables at build time | Medium | **Observed at M0.** Five artifacts fetched with no hash pinning; `deb` and `rpm` need none. Folded into open decision 7 — either drop AppImage or vendor and pin, before M2 |

R9 has a procurement lead time, so it cannot wait for M4. Revisit when M2 begins.
