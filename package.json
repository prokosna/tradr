{
	"name": "tradr",
	"private": true,
	"type": "module",
	"scripts": {
		"codegen": "buf generate",
		"lint": "pnpm codegen && biome lint --error-on-warnings .",
		"format": "biome format --write .",
		"typecheck": "pnpm codegen && tsc -p tsconfig.json",
		"test": "echo 'no test runner configured yet' && exit 0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.10",
		"@bufbuild/buf": "1.72.0",
		"@types/node": "26.2.0",
		"ts-proto": "2.12.1",
		"typescript": "7.0.2"
	}
}
