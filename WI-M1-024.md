## WI-M1-024: Listener half of the composition root

### Target
crates/tauri-plugin-tradr/src/listener.rs
crates/tauri-plugin-tradr/src/lib.rs

### Design to consult
- docs/04-protocol.md, "The Offer exchange"
- docs/03-discovery-and-transport.md, "Transports"

### Definition of done
- [ ] Implement a listener task that accepts a channel from `Incoming`
- [ ] Run `perform_handshake` on the Control pair
- [ ] Read a `TransferOffer` from the Control stream using the codec from `WI-M1-023`
- [ ] Determine the resume position by deriving `ItemResumption` from what is on disk
- [ ] Answer with a `TransferAccept` carrying the resume position. Any offered item not named is declined
- [ ] Drive `receive_file` per accepted Item
- [ ] Run `cargo fmt` and `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] Test: provable against a hand-driven sender with no UI in existence

### Constraints
- Do not modify `tradr-core` or `tradr-proto`. If that becomes necessary, raise a DCR and stop
- Respect the dependency directions in the Constraints

### Prohibitions
- Editing docs/
- Editing STATE.md, AGENTS.md, or CLAUDE.md
- Committing
- Anything absent from the definition of done
\n### Review Findings (REVISE)\n- The listener currently calls `channel.open_bi()` to create the data streams. According to docs/04-protocol.md, the **sender** opens the data streams and the **receiver** accepts them. The listener is the receiver, so it must call `channel.accept_bi()`.\n- The tests in `tests/listener.rs` also have this reversed: the mock sender task is calling `accept_bi()`. The mock sender must call `open_bi()` and the listener must call `accept_bi()`.\n- Fix these inverted calls in both `src/listener.rs` and `tests/listener.rs` and ensure `cargo test --workspace` passes.
\n- To allow `derive_item_resumption` to `stat` the partial file, you must also modify `crates/tradr-vfs/src/posix.rs`. In `check_deny_list`, change `is_denied(&components)` to `is_denied_for_write(&components)`. This allows `stat` on exact partial files while still keeping the `.tradr-partial` directory itself deny-listed from `list` and `stat`.
